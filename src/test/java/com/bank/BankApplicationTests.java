package com.bank;

import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;

@SpringBootTest
class BankApplicationTests {

    @Test
    void contextLoads() {
        // This method is intentionally left unimplemented because the @SpringBootTest
        // annotation already verifies that the Spring context loads successfully.
        throw new UnsupportedOperationException("This method is intentionally left unimplemented. " +
                "The @SpringBootTest annotation ensures the Spring context is loaded.");
    }
}